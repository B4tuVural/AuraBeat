# =============================================================
# AuraBeat — Python bağımlılıkları
# Django 5.2 LTS hattı (güvenlik güncellemeleri Nisan 2028'e kadar)
# =============================================================

# Web Framework
Django==5.2.4
djangorestframework==3.16.0
django-cors-headers==4.4.0

# Database — Modern psycopg 3 (Django 5.2'nin tercih ettiği sürücü)
# psycopg2-binary'ye gerek yok. `[binary]` extra'sı libpq'yu wheel'a gömer,
# bu sayede ek sistem paketi (`libpq-dev`) gerekmez.
psycopg[binary]==3.2.3

# AI Integration
google-generativeai==0.8.3

# Konfigürasyon
python-dotenv==1.0.1

# Opsiyonel: Spotify (Music Provider)
spotipy==2.24.0
